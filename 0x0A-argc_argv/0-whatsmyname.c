#include <stdio.h>
/**
 * main - this is the main function
 * @argc: one arguments
 * @argv: the arguments size
 * Return: return nothing at this point
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s", argv[0]);

	return (0);
}
