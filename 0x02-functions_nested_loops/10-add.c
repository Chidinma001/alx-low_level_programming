#include "main.h"

/**
 * add - add two integers and return the result
 * @a: first parameter
 * @b: secon parameter
 * Return:sum of the two parameters
 */
int add(int a, int b)
{
	return (a + b);
}
